/**
 * Item Registry - defines ID, name, texture
 * and certain other properties of custom items.
 */
StartupEvents.registry('item', event => {

    // PhoenixWare Ingots/Boules/Dusts (Raw Useable materiels)


}); // <- Removed the extra closing brace
